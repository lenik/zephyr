# zephyr

**D** + Meson template (`ldc2` or `dmd`).

```bash
sudo apt install meson ninja-build ldc asciidoctor
meson setup /build && ninja -C /build && meson test -C /build
```

AGPL-3.0-or-later — see `LICENSE`.
