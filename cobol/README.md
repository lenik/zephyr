# zephyr

**COBOL (GnuCOBOL)** + Meson template.

```bash
sudo apt install meson ninja-build gnucobol asciidoctor
meson setup /build && ninja -C /build && meson test -C /build
```

AGPL-3.0-or-later — see `LICENSE`.
