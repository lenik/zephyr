# zephyr

**Fortran** + Meson template (`gfortran`).

```bash
sudo apt install meson ninja-build gfortran asciidoctor
meson setup /build && ninja -C /build && meson test -C /build
```

AGPL-3.0-or-later — see `LICENSE`.
