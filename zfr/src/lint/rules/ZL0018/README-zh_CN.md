# meson.build 的 project() 调用

### Meson 是权威构建系统

身份、许可证、版本来源、手册页、补全以及 look/posync 目标都在 meson.build。此处漂移会同时破坏 Debian 与 RPM。


### 本检查：{title}

{detail}


### 编辑提示

Ize 会修补 meson.build；将自定义目标与模板块对齐，大改后重新 configure。
