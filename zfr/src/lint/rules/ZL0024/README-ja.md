# Meson run_target look の DESTDIR プレビュー

### Meson が正式なビルドシステムです

識別子、ライセンス、バージョン源、man、補完、look/posync ターゲットは meson.build に置きます。ここでのずれは Debian も RPM も壊します。


### この検査: {title}

{detail}


### 編集のヒント

Ize は meson.build をパッチします；カスタムターゲットをテンプレートブロックと調停し、大きな編集後は再 configure。
