#!/usr/bin/env bash
# upload_vsix — optional marketplace / private feed upload.
# Customize for your registry; default warns and exits 0.
set -euo pipefail
echo "upload_vsix: not configured (edit ~/.config/zfr/scripts/upload_vsix)" >&2
exit 0
