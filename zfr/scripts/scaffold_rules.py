#!/usr/bin/env python3
# SPDX-License-Identifier: AGPL-3.0-or-later
"""One-shot historical scaffolder (already applied).

Rule dirs now live under src/lint/ZL* and src/ize/ZI*. This script is kept
only as a reference for how catalogs were split; it no longer regenerates
from std.* catalogs (those are discovery wrappers).
"""

from __future__ import annotations

raise SystemExit(
    "scaffold_rules.py already ran; edit lint/ZL*/ or ize/ZI*/ directly."
)
