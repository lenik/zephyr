zephyr (2.8.8) testing; urgency=medium

  * i18n derive: treat equal mtimes as fresh (>=), matching msgfmt 0.23 which
    stamps .mo with the source .po mtime; no forced mtime bumps.
  * OpenCC: resolve config under /usr/share/opencc; skip conversion test when
    CLI missing; Build-Depends/Recommends opencc + gettext.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 01 Sep 2026 03:40:00 +0800

zephyr (2.8.7) testing; urgency=medium

  * lint ZL083: check root/component .gitignore (node_modules/, dist/,
    backend/src/generated/, web/, mobile/, prisma/, e2e/).
  * zfr: global --for-ai-purpose/--no-for-ai-purpose (auto for AI terminals
    and non-interactive stdout); lint prints an AI dirty-tree hint to update
    changelog and commit verbosely after PASS.
  * ize: completion stubs only for real CLI puffs (docs man1 / executable /
    *.bash) — skip src modules and lib*.so man stems (audiocfg).

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 01 Sep 2026 01:47:00 +0800

zephyr (2.8.6) testing; urgency=medium

  * i18n derive: drop .derive.sig sidecars; use mtime freshness for derived
    .po/.mo and man pages; absolute Meson stamp path; install flat build/po
    *.po catalogs.
  * ize/lint: read legacy .po encodings and rewrite catalogs as UTF-8
    (charset header + no-wrap); move RPM spec sync after i18n so locale
    man globs are included on first run.
  * ize: foreach man_puffs in RPM %files; dedupe doc paths; debian/control
    Priority optional, extended Description, empty Depends bash-shlib fix;
    fallback debian/copyright from c/cpp/bash when lang template lacks it.
  * lint: debian Priority/Description checks; Java source.long threshold
    2000; D projects accept test/ or tests/ (lang.d.tests is a note).

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 31 Aug 2026 18:16:00 +0800

zephyr (2.8.5) testing; urgency=medium

  * lint ZL079: map debian/zephyr.substvars to <project>.substvars; skip
    debian build leftovers.
  * ize: Meson man/completion via foreach; do not re-append duplicate
    *-man targets when an apps foreach already exists.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 21:01:10 +0800

zephyr (2.8.4) testing; urgency=medium

  * ize i18n-derive: Meson builds and installs derived .mo (ZI015).
  * lint/ize: -L/--list-std table and -H/--help-std NUM; drop
    docs/lint-std.md and docs/ize-std.md.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 20:27:52 +0800

zephyr (2.8.3) testing; urgency=medium

  * ize/lint: batch1-3 hardening — remove project rpmbuild; skip man
    locales without po/; accept root *.in and completions/; completion
    stubs under completions/ (keep lang detect); fix Architecture/noarch
    for script packages; tolerate bash+ELF hybrids.
  * source.long on ize/engine/__init__.py (605 lines)
    moved i18n helpers into ize/engine/i18n.py

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 19:43:56 +0800

zephyr (2.8.2) testing; urgency=medium

  * ize: ensure LINGUAS/.po and docs/<locale> man scaffolds for lint
    l10n level (ZI018/ZI019); expand packaging SCAFFOLD (common.mk,
    win32 gitignore/build-host.example, rpm/.gitignore); fix
    derive_i18n_locales to run when not dry-run.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 19:32:26 +0800

zephyr (2.8.1) testing; urgency=medium

  * RPM %%files for the zephyr package: list zfr bindir wrappers, share/zephyr/,
    and gettext zephyr.mo (was missing paths / wrongly expecting zfr.mo).
  * Gettext domain aligned with the package: po/zfr.pot → po/zephyr.pot;
    i18n.gettext / runtime _DOMAIN = zephyr.
  * ize meson_rpm_files: detect wrapper_names→bindir, i18n.gettext domain,
    and pkgdatadir installs (so %%files stays in sync with Meson).

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 18:50:00 +0800

zephyr (2.8.0) testing; urgency=medium

  * Packaging win32 overhaul (all language templates + meta packaging/ + zfr):
    rename packaging/windows/ to packaging/win32/ with complete recipes —
    mingw (<pkg>_mingw-<ver>.exe), innosetup (<pkg>-<ver>.exe), wix (<pkg>-<ver>.msi).
    Inno Setup / WiX native recipes are build.cmd + build.ps1 (cmd.exe / PowerShell).
  * Shared packaging/lib/host.sh + common.mk: can-local / run / dump for native or
    cross tools on this host only. Remote .build-host sync+SSH+fetch+clean is no
    longer in host.sh; gh-makerelease owns that path (project then $HOME
    .config/zephyr/<packaging>.build-host; last hop build_dir / shell / preserved).
  * Local cross where tools exist: mingw-w64 for mingw, wine+ISCC for Inno Setup,
    optional wixl for MSI; macos/arch/freebsd Makefiles use the same local dispatch.
  * zfr: packaging_host.py parser + tests; ize copies packaging/lib and win32
    scaffold files from the language template.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 18:25:00 +0800

zephyr (2.7.8) testing; urgency=medium

  * Packaging layout: move rpm/ to packaging/rpm/ across all language templates
    and the meta-repo; ize/lint migrate legacy rpm/ automatically. Makefile
    SRCDIR fixed for packaging/rpm depth.
  * Add optional packaging stubs: windows/, macos/, arch/ (PKGBUILD), freebsd/;
    ize scaffold copies from the language template.
  * Remove scripts/apply_l2_new_translations.py from repository history (one-off
    L2 translation helper, never meant to ship).

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 17:20:00 +0800

zephyr (2.7.7) testing; urgency=medium

  * PO catalogs: no line wrapping (msgcat --no-wrap). Lint ZL082 warns on wrapped
    source .po files; ize ZI016 rewrites them. Derived catalogs follow the same rule.
  * Derived locales at build time: zfr i18n -b writes child .po under build/po/ only
    (Derived-From / Derived-Method headers). Meson custom_target (ize ZI015) runs
    derive on source .po/LINGUAS changes; SHA256 .derive.sig cache skips unchanged
    parents. --compile-mo builds <lang>/LC_MESSAGES/<domain>.mo in the build dir.
  * zfr i18n -a lists primaries plus build-time derived locales; --po-dir, --stamp,
    --domain, --compile-mo for Meson integration.
  * Fix --domain override: explicit --domain is no longer replaced by meson project()
    name (zephyr vs zfr gettext domain).
  * Tests: po_format, meson derive hook; l10n tiers include derived build locales.
  * rpm: debug_package nil; expand bash-completion file list; locale path fix.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 17:05:00 +0800

zephyr (2.7.6) testing; urgency=medium

  * L2 locale catalogs (ar, de, es, fr, hi, id, it, ja, ko, nl, pl, pt, ru,
    th, tr, vi, zh_CN): translations for new i18n/translate CLI strings.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 16:26:00 +0800

zephyr (2.7.5) testing; urgency=medium

  * Split i18n and translate into subpackages (zfr_lib/i18n/, zfr_lib/translate/);
    standalone zfr-i18n and zfr-translate entry points; zfr t prefix alias.
  * Numbered lint (ZL001–ZL081) and ize (ZI001–ZI016) standard rules; docs in
    docs/lint-std.md and docs/ize-std.md; suppress with -u/--uncheck or
    .config/zfr/lint.options / ize.options.
  * Lint report shows rule IDs; notes use note severity color; style-info block
    defaults on for pipes/CI and AI-integrated terminals (Cursor, VS Code, …).
  * zfr translate: -k/--keys, -m/-M match (ci/cs), -s/-S search (ci/cs),
    -E/--entire for full-string regex; grouped output by msgid.
  * zfr i18n -b/--build: derive child locales only when absent from po/LINGUAS.
  * L10n tiers: L1=8 primaries, L2=+17, L3=+35; LANGS documents locale tree.
  * Refactor: split ize/engine.py into engine/ subpackage; lint rpm_util.py;
    tests reorganized under tests/{cli,i18n,ize,l10n,rpm}/.
  * xgettext template (po/zfr.pot) updated for new CLI strings.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 30 Aug 2026 16:24:00 +0800

zephyr (2.7.4) testing; urgency=medium

  * ize/release: script-only packages (python/java/…) get BuildArch noarch
    and %global debug_package %{nil} (fixes empty debuginfo on 2meson/echo).
  * meson_rpm_files: detect install_subdir(... install_dir: bindir); only list
    setup/ when Meson installs it.
  * zfr dist: fall back to project-tree archive when meson.build is untracked
    (post-ize dirty trees).
  * shape: Autotools/CMake/Maven roots without debian/ are package candidates.
  * detect: m4-macro data packages score as bash; debhelper-compat (= 13).
  * language rpm/Makefile: bootstrap probe is tools/zfr (not tools/zephyr).
  * lint: rpm.noarch.script error when script packages lack BuildArch noarch.

 -- Lenik (谢继雷) <lenik@bodz.net>  Thu, 27 Aug 2026 18:38:34 +0800

zephyr (2.7.3) testing; urgency=medium

  * meson_rpm_files: detect Python ``custom_target`` bindir installs,
    ``pypkgdir`` (dist-packages), and bare ``install_dir: pkgdatadir``;
    fixes RPM unpackaged files on python projects like cutekeys.
  * Do not treat ``install_dir: pkgdatadir / 'sub'`` as top-level
    ``%{_datadir}/%%{name}/*`` (zephyr meta-package layout).

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 23:43:13 +0800

zephyr (2.7.1) testing; urgency=medium

  * ize --commit: bump patch version and prepend debian/changelog (plus
    sync VERSION) before git add/commit, so batch `zfr ize -c` leaves a
    versioned packaging commit, not only style file edits.
  * ize --commit: reuse the previous debian/changelog trailer author by
    default (not debian/control Maintainer); override with
    ``--author='Name <email>'`` or ``--author=Name`` (keeps prior email).
  * Commit subject includes the new version; changelog bullets summarize
    each add/update/convert from the ize run.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 23:15:56 +0800

zephyr (2.7.0) testing; urgency=medium

  * Refactor: unify RPM %_topdir on $HOME/rpmbuild (rpm --eval '%{_topdir}';
    override via ~/.rpmmacros), not <project>/rpmbuild.
  * All language rpm/Makefile templates + zfr/rpm/Makefile: default TOPDIR from
    rpm macros; clean removes only this package's SPECS/SOURCES/RPMS/SRPMS/BUILD
    artifacts (no longer rm -rf the whole topdir).
  * zfr dist --rpm: write SOURCES under %_topdir; help/docs updated.
  * zfr lint/ize: flag and migrate project-local TOPDIR in rpm/Makefile.
  * Fix test failures from %_topdir help text: escape %% for argparse.
  * meson_rpm_files: parse foreach + rename:var bash-completion installs
    (was falsely requiring zfr.bash in %files).
  * lint: treat completions/foo-* globs as covering foo-bar entries.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 22:37:50 +0800

zephyr (2.6.4) testing; urgency=medium

  * lint/rpm: rewrite broken check_rpm (early return left most checks dead);
    compare %files to Meson install set (bindir, completions, manN, pkgdata,
    perl5, includedir, sysconfdir, shlib.d, setup/, gettext .mo, locale mans).
  * lint/rpm: error on BuildArch: noarch when Meson has executable(); on Debian
    substvars (${…}) left in Requires; on spurious %{_datadir}/%{name}/;
    on bash-completion basename mismatches (e.g. coolutils vs coolutils.sh).
  * lint/debian: error Architecture: all when Meson has executable() (same
    class of bug as RPM BuildArch: noarch).
  * Catches the failure modes seen under ``zfr release -l -fIR`` before the
    build, matching what ``zfr ize`` now repairs.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 22:33:54 +0800

zephyr (2.6.3) testing; urgency=medium

  * lint: add -i/--info and -I/--no-info to show or hide the zephyr
    style-contract / next-steps / hint blurbs.
  * Default: show style info when stdout is non-interactive (pipes/AI);
    hide it on an interactive TTY. -i/-I override the auto choice.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 22:24:57 +0800

zephyr (2.6.2) testing; urgency=medium

  * ize: add -c/--commit — after a successful ize, ``git add -A`` and
    commit with a verbose message listing each add/update/convert and
    a summary (N added, M updated, K converted). Refuses --dry-run.
  * Useful for batch trees: ``zfr ize -c`` leaves a reviewable commit
    instead of a dirty working tree.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 22:19:53 +0800

zephyr (2.6.1) testing; urgency=medium

  * ize: derive RPM %files from Meson install rules (not docs/puff guesses),
    covering bindir, bash-completion (single/list/configure_file), man1–9
    including install_man and dotted stems (e.g. m2deploy.cfg.5), pkgdata,
    perl5, includedir, sysconfdir, shlib.d, bash-alias, setup/, gettext .mo.
  * ize: when Meson defines the install set, skip lang-template extras such as
    Java %{_datadir}/%{name}/ that invent empty dirs rpmbuild rejects.
  * ize: drop BuildArch: noarch / debug_package %{nil} when Meson has
    executable() (ELF packages wrongly marked noarch after earlier ize).
  * ize: strip Debian ${…} substvars from RPM Requires (and from dep tokens
    when syncing from debian/control).
  * sync_rpm_files rewrites %files whenever the Meson-derived set differs.
  * Validated zfr release -l -fIR --unsigned on pick batch1–3: 40/42 pass;
    ai-utils (missing ai.c) and catme (broken headers) remain project bugs.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 22:09:44 +0800

zephyr (2.6.0) testing; urgency=medium

  * lint: error when RPM %files omits gettext .mo, locale mans, or
    datadir/setup scripts that Meson installs (unpackaged after
    `zfr release -fI`).
  * lint: error on leftover some_puff1 / template puff in %files.
  * ize: rewrite %files for template puff; append .mo / locale-man /
    setup/ globs; bash specs get BuildArch noarch + %global debug_package
    %{nil}.
  * ize: Build-Depends continuation commas; debian/rules
    override_dh_autoreconf; fix debian/docs README→README.md; per-puff
    bash-completion stubs after man discovery; invoke 2meson -r.
  * Aligns ize + lint with `zfr release -fI` (Debian + RPM); validated on
    batch1 2tree through full local release.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 21:14:00 +0800

zephyr (2.5.0) testing; urgency=medium

  * ize: patch existing debian/control for lint — Build-Depends meson,
    ninja-build, asciidoctor; Architecture; bash Depends bash-shlib;
    normalize blank lines so Source/Package stanzas parse correctly.
  * ize: emit docs/*.adoc man stubs (+ asciidoctor targets) when Autotools
    mans were help2man-only; strip help2man blocks from meson.build.
  * ize: strip template README banners on copy; add bash-completion stub
    before meson patch so install_data is wired; RPM Requires bash-shlib.
  * lint: resolve Package stanza by Package: key (not stanzas[1]).
  * Validated ize + lint (0 errors) + meson setup on batch1 (5/6 setup;
    ai-utils missing upstream ai.c) and batch2 (12/12).

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 20:40:00 +0800

zephyr (2.4.1) testing; urgency=medium

  * zfr lint: wrap all finding, fix, and report strings with gettext
    (_()).
  * Use named python-format placeholders for interpolated lint messages
    so translators can reorder arguments.
  * Translate the new msgids in all LINGUAS catalogs; add
    test_lint_zh_cn_and_de.

 -- Lenik (谢继雷) <lenik@bodz.net>  Tue, 25 Aug 2026 18:19:46 +0800

zephyr (2.4.0) testing; urgency=medium

  * ize convert_manpages: convert groff sections other than .1; skip
    autom4te.cache; install_dir manN from the adoc title.
  * strip_install_man_paths: drop leftover docs/*.adoc from install_man()
    after conversion (Meson only accepts numeric-section man sources).
  * Validated one-pass ize --mesonize + meson setup on batch2 (autover,
    bash-shlib, brightness, btshare, catme, cdtree, configutils, coolapk,
    coolauto, coolbin, coolclip, coolcmd).

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 18:19:11 +0800

zephyr (2.3.0) testing; urgency=medium

  * zfr ize: add -m/--mesonize (default on; --no-mesonize to skip) to run
    2meson when Autotools/CMake sources are present, before scaffolding.
  * Fix patch_meson project() parsing: string-aware parenthesis matching so
    multi-line version: run_command(...) is not truncated; accept
    "version :" spacing; safer license injection before the closing paren.
  * Document mesonize in en/zh_CN manpages and bash completion; add ize
    mesonize CLI test; gettext string for the new option across LINGUAS.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 15:20:00 +0800

zephyr (2.2.2) testing; urgency=medium

  * L3 locale set: replace Slovak/Slovene (sk, sl) with Vietnamese and
    Ukrainian (vi, uk).
  * Add Ukrainian gettext catalog (po/uk.po) and whole-document manpage
    (docs/uk/zfr.adoc); register uk in po/LINGUAS and meson man_i18n_langs
    (vi was already present).
  * Document the L3 swap in en, zh_CN, and L2 manpages (bg, ca, cs, el, hu,
    nl, pl).
  * lint template.coverage: skip commons / test names; map rpm/zephyr.spec
    to rpm/<project>.spec in findings.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 10:45:00 +0800

zephyr (2.2.1) testing; urgency=medium

  * Add language templates: pascal (Free Pascal), fortran (gfortran), cobol
    (GnuCOBOL), and d (D/ldc); register lang/*.py specs and bash completion.
  * Extend zfr detect/create tests; document all 31 templates in en/zh manpages.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 01:40:00 +0800

zephyr (2.2.0) testing; urgency=medium

  * Add language templates: lua, zig, kotlin, elixir, nim, ocaml, antlr
    (ANTLR4 parser apps), bison (flex+bison parser apps), as (NASM), gcc
    (GNU C extensions).
  * Parser templates (antlr, bison): default format rewrite; -d/--dump AST,
    --indent-size, -C/--color; extend zfr detect/create tests.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 01:10:00 +0800

zephyr (2.1.2) testing; urgency=medium

  * Extract per-language zfr logic into zfr_lib/lang/<lang>.py (detection,
    puff paths, meson wiring, lint bits, RPM spec extras); remove monolithic
    lang.py; resolve import cycles via zfr_lib/finding.py.
  * Rename template example shared module common_lib → commons across all
    language trees (clib/cpplib keep lib.c/lib.cpp).
  * zfr lint: skip line-count on example commons modules; warn >1000 lines,
    note >600; note/warn on legacy common_lib names.
  * zfr ize: RPM spec file lists delegate to lang spec_files_fn.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 24 Aug 2026 01:05:00 +0800

zephyr (2.1.1) testing; urgency=medium

  * Move zfr/README.md and zfr/README-zh.md to the meta-repo root; install
    them from there in meson; lint and shape resolve README at repodir for
    the zfr CLI package.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 21:13:17 +0800

zephyr (2.1.0) testing; urgency=medium

  * Consolidate Debian packaging under zfr/debian/ only (drop repo-root
    debian/ and the zfr/debian symlink); keep meta-repo hooks in
    /.githooks only (remove zfr/githooks).
  * Add zfr lint -l/--l10n-level L0–L3 (default L1): whole-document
    docs/<locale>/zfr.adoc and complete .po catalogs per tier; zfr itself
    requires L2 via .config/zfr/lint.options.
  * Add L2 locale content: bg, ca, cs, el, hu, nl, pl (AsciiDoc manpages
    and gettext catalogs); expand LINGUAS and meson man_i18n_langs.
  * zfr create: default --author/--email from git config user.name and
    user.email (fallback to package defaults).
  * Refresh README and manpage docs for l10n-level; extend tests (44).

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 20:47:47 +0800

zephyr (2.0.1) testing; urgency=medium

  * Checkpoint: rewrite Tamil, Urdu, Bengali, and Telugu CLI/man
    translations as native prose (not English calques).

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 20:05:00 +0800

zephyr (2.0.0) testing; urgency=medium

  * zfr --version reads git describe / changelog / VERSION (not a
    hardcoded string in src/zfr).
  * Expand tests to cover every subcommand and zfr-* wrapper, language
    scoring, gettext completeness, and per-locale manpage sources.
  * Ship whole-document AsciiDoc translations under docs/<lang>/zfr.adoc
    for all recommended locales; drop po4a/po/man catalogs.
  * Complete native gettext translations for every LINGUAS catalog.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 19:45:00 +0800

zephyr (1.10.0) testing; urgency=medium

  * Split zfr commands into per-command modules; argparse lives with each
    command (zfr is only a dispatcher). Modularize lint and ize.
  * Detect language with a built-in confidence vector over project files
    (Depends/Build-Depends only; Description is ignored).
  * Fix CLI gettext: find compiled .mo catalogs (including source po/
    via msgfmt) so `LANGUAGE=zh_CN zfr -h` is translated.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 18:40:00 +0800

zephyr (1.9.0) testing; urgency=medium

  * Add zfr-release / zfr release: parse gh-makerelease options and exec
    gh-makerelease. Treat the zfr CLI package as python/package (not a
    typescript language template) so zfr lint is clean. Ship debian/ at
    the git root so gh-makerelease can release the zephyr meta-package;
    Meson source is zfr/. Dist archives the meta-repo so language
    templates are included.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 16:25:53 +0800

zephyr (1.8.0) testing; urgency=medium

  * Relocate the zfr CLI from tools/ to zfr/ as its own Meson project.
    Language templates stay as siblings in the checkout; uninstalled
    wrappers set ZFR_PKGDATADIR to the repo root and PYTHONPATH to
    zfr/src, and installed scripts live under share/zephyr/zfr/.
  * zfr lint walks up to .git, honors core.hooksPath / .githooks, and
    checks that pre-commit syncs VERSION from debian/changelog (so a
    packagedir without its own .githooks is fine in a monorepo).
  * Add po4a man-page translations (po/man/, LINGUAS). zh_CN and zh_TW
    are complete and install to share/man/<locale>/man1/zfr.1; other
    locales ship as translator stubs.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sun, 23 Aug 2026 12:01:10 +0800

zephyr (1.7.0) testing; urgency=medium

  * Recommend gettext LINGUAS coverage: ar bn de es fr hi id it ja ko pt
    ru sv ta te th tr ur vi zh_CN zh_TW (en source; zh-cn/zh-tw
    aliases).
  * zfr lint warns when po/LINGUAS or .po files miss that set; expand
    language-template catalogs and document in zfr(1)/README.
  * Monorepo layout: distinguish repodir vs packagedir via package shape
    scoring.
  * Add zfr shape / zfr-shape (0-100 completion score; -b/--bool prints
    0 or 1).
  * Add gettext catalogs for the zfr CLI tools.
  * Fix go template debian packaging to Meson dh style; sync VERSION to
    changelog without bumping template release.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sat, 22 Aug 2026 00:34:32 +0800

zephyr (1.6.0) testing; urgency=medium

  * Rename the CLI tool from zephyr to zfr; keep zephyr as the template
    placeholder that create/rename replace.
  * Document that instantiated projects should call zfr (version, lint,
    dist, ize); zfr is not a magic rename token.

 -- Lenik (谢继雷) <lenik@bodz.net>  Sat, 22 Aug 2026 00:22:22 +0800

zephyr (1.5.0) testing; urgency=medium

  * Add parent tests/ covering zephyr subcommands with temporary
    example projects (create, add, remove, rename, about, version,
    lint, dist, ize, detect). meson test runs unittest discover;
    tests are not installed.
  * Refresh language gettext catalogs (xgettext / msgmerge) for
    templates that extract from real sources (python, java,
    typescript, clib, cpplib, rust). Expand the Chinese README to
    match the English install/layout/tests sections.

 -- Lenik (谢继雷) <lenik@bodz.net>  Thu, 20 Aug 2026 09:15:00 +0800

zephyr (1.4.0) testing; urgency=medium

  * Add `zfr about`, `zfr version`, and `zfr lint` (and
    zfr-about / zfr-version / zfr-lint wrappers). Each walks
    from the current directory to the project root (meson project() or
    debian/control).
  * `zfr version` prefers git describe when git and .git exist, else
    debian/changelog, then a VERSION file, then 0.0.0. `-r` maps hyphens
    to underscores for RPM. Meson and rpm/Makefile call `zfr` on PATH
    and fall back to VERSION (not a relative tools/zfr path).
  * `zfr about` prints parsed common fields from debian or RPM
    (section, priority, distribution, architecture, and so on) wrapped
    to the terminal, plus source file/line counts and an estimated
    payload size. `-d`/`-r` reformat packaging metadata rather than
    dumping files. CSR colors on a TTY.
  * `zfr lint` checks packaging and layout against zephyr style
    (missing files, identity, Meson/Debian/RPM, leftover template
    tokens) and prints coded findings with fixes. CSR colors on a TTY.
  * Snapshot the changelog version in VERSION (allowed to differ from
    git describe). .githooks/pre-commit syncs VERSION from
    debian/changelog; `zfr create` sets git core.hooksPath to
    .githooks.
  * Add parent rpm/ packaging (Meson, noarch, aligned with
    debian/control). Track rpm/*.spec. `zfr create` copies the
    VERSION pre-commit hook into new projects.
  * Refactor bash RPM packaging to match debian/control (Meson, noarch,
    AGPL-3.0-or-later, bash-shlib).
  * Add `zfr dist` (and zfr-dist). Uses `meson dist` when the
    project is the git root (`--allow-dirty`, `--no-tests` by default);
    nested language templates pack only that directory. Prints the
    archive path on stdout. rpm/Makefile `dist` and the Meson `srcdist`
    ninja target call this command.
  * Apply the same versioning and RPM layout to the remaining language
    templates (not only bash). meson.build uses `zfr version`, then
    VERSION, then 0.0.0. Each language ships rpm/zephyr.spec aligned
    with debian/control (dynamic %{version}, Meson, AGPL) and
    rpm/Makefile calling `zfr dist --rpm`.
  * Add `zfr ize` (and zfr-ize). In-place upgrade of an existing
    project to current zephyr style: missing debian/rpm files from the
    language template, VERSION and pre-commit hook, meson.build patches
    (`zfr version`, asciidoctor man targets, `look`, completions),
    groff `.1` → `docs/*.adoc`, and hardcoded src versions → `@VERSION@`
    / `config.h`. `-n` dry-run. Then `zfr lint`.

 -- Lenik (谢继雷) <lenik@bodz.net>  Thu, 20 Aug 2026 01:33:00 +0800

zephyr (1.3.1) testing; urgency=medium

  * Fix create/add so named projects leave no puff1 or zephyr tokens
    (rewrite debian/, strip packaging leftovers, instantiate
    puff+project in one pass).
  * Point uninstalled builddir wrappers (build/zephyr) at the source
    tree so ninja picks up current tools and templates without meson
    install.

 -- Lenik (谢继雷) <lenik@bodz.net>  Mon, 17 Aug 2026 15:32:52 +0800

zephyr (1.3.0) testing; urgency=medium

  * Strip C++ from the clib template (C-only library).
  * Add cpp (simple C++ CLI) and cpplib (C++ shared/static library) templates.
  * Rename language template cs to csharp.
  * Add ruby CLI template.
  * Update tools, bash-completion, README, and manpage language lists.

 -- Lenik (谢继雷) <lenik@bodz.net>  Fri, 14 Aug 2026 22:09:43 +0800

zephyr (1.2.0) testing; urgency=medium

  * AsciiDoc man pages for language templates.
  * zfr create subcommand.
  * Multi-puff add/remove support.
  * Rename c template to clib.
  * New c/bash/perl templates.
  * Tool man pages, bash-completion, and README.

 -- Lenik <zephyr@bodz.net>  Fri, 14 Aug 2026 21:38:17 +0800

zephyr (1.1.1) testing; urgency=medium

  * Snapshot before asciidoc/man, create subcmd, clib/c/bash/perl refactor.

 -- Lenik <zephyr@bodz.net>  Fri, 14 Aug 2026 21:37:03 +0800

zephyr (1.1.0) testing; urgency=medium

  * Rename puff1 example to some_puff1 across templates.
  * Install language templates under /usr/share/zephyr/<lang>/.
  * Add zephyr CLI (rename/add/remove) as Python cmdline tools.

 -- Lenik <zephyr@bodz.net>  Fri, 14 Aug 2026 20:21:03 +0800

zephyr (1.0.1) testing; urgency=medium

  * Snapshot before puff1→some_puff1 refactor and top-level packaging.

 -- Lenik <zephyr@bodz.net>  Fri, 14 Aug 2026 20:20:56 +0800
